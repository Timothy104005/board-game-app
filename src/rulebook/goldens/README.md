# Rulebook Goldens

These files are deterministic snapshots for rulebook front-end outputs.

Each golden JSON contains:

- `recognizer`
- `irDraft`
- `gapSummary`
- `gaps`
- `checker`
- `compiledMeta`

If behavior changes intentionally, regenerate and review these files in the same change.
